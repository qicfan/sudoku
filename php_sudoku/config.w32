// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("sudoku", "for sudoku support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("sudoku", "enable sudoku support", "no");

if (PHP_SUDOKU != "no") {
	EXTENSION("sudoku", "sudoku.c");
}

